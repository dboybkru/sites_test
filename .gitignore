# dependencies
node_modules/

# build output
dist/
.astro/
showcase/public/s/

# deploy / tooling
.vercel/
.env
.env.*
!.env.example

# key/secret files dropped into the project — NEVER commit
*.key
*.pem
*ключ*
*secret*.txt
*key*.txt

# graphify (large, regenerated; lives in parent but ignore defensively)
graphify-out/
.graphify_*

# raw dropped images (huge originals; optimized webp is committed in sites/*/public/img)
assets-incoming/*
!assets-incoming/README.md

# os / editor
.DS_Store
Thumbs.db
*.log
